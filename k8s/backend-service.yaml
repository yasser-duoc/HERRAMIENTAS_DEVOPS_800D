apiVersion: v1
kind: Service
metadata:
  name: tienda-backend
  namespace: tienda
spec:
  selector:
    app: tienda-backend
  ports:
    - port: 3001
      targetPort: 3001
  type: ClusterIP
